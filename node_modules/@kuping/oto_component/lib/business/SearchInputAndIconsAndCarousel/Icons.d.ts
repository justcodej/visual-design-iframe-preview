import { Component } from 'react';
interface Props {
    iconsData: Array<ItemProps>;
    onClick?: (path: string) => void;
}
interface ItemProps {
    imgUrl: string;
    pathUrl?: string;
    name: string;
    iconLabel?: string;
}
declare class Icons extends Component<Props> {
    static defaultProps: Props;
    handleClick(config: any): void;
    render(): JSX.Element;
}
export default Icons;

import { Component } from 'react';
import './style/list.less';
interface Props {
    data: DataProps;
    monitor?: MonitorProps;
}
interface MonitorProps {
    monitorName: string;
    componentName: string;
    is: boolean;
}
interface DataProps {
    cardId: string;
    denominationId: string;
    denominationImage: string;
    commodityPic: string;
    denominationTitle: string;
    disAmount: number;
    maxDiscount: number;
    originalPrice: string;
    sale: string;
}
declare class Coupon extends Component<Props> {
    constructor(props: any);
    goCardDetail(id: any, denominationId: any): void;
    render(): JSX.Element;
}
export default Coupon;

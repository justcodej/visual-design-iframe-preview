import { Component } from 'react';
import './index.less';
interface Props {
    data: IObjData;
    onClick?: (path: string) => void;
}
declare class Heading extends Component<Props> {
    static defaultProps: {
        attr: {
            name: string;
        };
    };
    handleClick(config: any): void;
    render(): JSX.Element;
}
export default Heading;

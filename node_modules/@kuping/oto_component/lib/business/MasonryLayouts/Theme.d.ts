import { Component } from 'react';
interface Props {
    selected: SelectedItemProps;
    dataSource: Array<ItemProps>;
    onChange?: (item: ItemProps) => void;
}
interface SelectedItemProps {
    name: string;
    value: string | number;
}
interface ItemProps {
    name: string;
}
declare class Theme extends Component<Props> {
    themeWrapEl: any;
    selectedEl: any;
    selectedTheme(item: ItemProps): void;
    render(): JSX.Element;
}
export default Theme;

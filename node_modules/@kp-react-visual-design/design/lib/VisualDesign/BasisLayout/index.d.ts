import { FC, PropsWithChildren, ReactNode } from 'react';
export declare type BasisLayoutProps = {
    title: string;
    actions: ReactNode;
};
declare const BasisLayout: FC<PropsWithChildren<BasisLayoutProps>>;
export default BasisLayout;

import moment from 'moment';
var columns = [{
  title: '优惠券名称',
  dataIndex: 'templateName'
}, {
  title: '优惠券有效期',
  dataIndex: 'startTime',
  render: function render(_, _ref) {
    var startTime = _ref.startTime,
        endTime = _ref.endTime,
        validTimeType = _ref.validTimeType,
        intervalDays = _ref.intervalDays,
        validDays = _ref.validDays;
    return validTimeType === 1 ? "".concat(moment(new Date(startTime)).format('YYYY/MM/DD HH:mm:ss'), " - ").concat(moment(new Date(endTime)).format('YYYY/MM/DD HH:mm:ss')) : "\u9886\u53D6\u540E".concat(intervalDays === 0 ? '即时' : "".concat(intervalDays, " '\u5929'"), "\u751F\u6548,\u6709\u6548\u671F").concat(validDays, "\u5929");
  }
}];
export default columns;
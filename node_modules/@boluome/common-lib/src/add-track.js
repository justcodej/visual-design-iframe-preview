
export default (
  (arg1, arg2) => {
    const { _hmt = {} } = window
    if (_hmt) {
      console.log('add-track', arg1, arg2)
      _hmt.push(['_trackEvent', arg1, 'click', arg2])
    } else {
      console.log('no _hmt')
    }
  }
)
import { ComponentClass, CSSProperties } from 'react';
import base from './base';
export declare type shapeTypes = 'circle' | 'default';
export interface KpIconProps extends base {
    type: string;
    index?: number | undefined;
    onClick?: () => void;
    style?: CSSProperties;
    shape?: shapeTypes;
}
export interface KpIconGroupProps extends base {
    defalutIndex: number;
    onChange?: (index: number) => void;
}
declare const KpIcon: ComponentClass<KpIconProps>;
export default KpIcon;

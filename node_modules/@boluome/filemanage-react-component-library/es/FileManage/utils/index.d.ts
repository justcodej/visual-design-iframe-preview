import { IDataSourceItem } from '@/FileManage/types/file-manage';
export declare const getQueryObj: (urlStr?: string) => any;
export declare const calcURLPath: (path: string) => {
    selected: IDataSourceItem;
    breadcrumb: number[];
};

import React from 'react';
import classNames from 'classnames';
import { StyledIcon } from "./styled";
import { IconGroupContext } from "./..";

var KpIcon = function KpIcon(_ref) {
  var type = _ref.type,
      index = _ref.index,
      style = _ref.style,
      shape = _ref.shape,
      onClick = _ref.onClick;

  var handleClick = function handleClick(_ref2) {
    var onChange = _ref2.onChange;
    typeof index === 'number' && onChange(index);
    onClick && onClick();
  };

  return /*#__PURE__*/React.createElement(IconGroupContext.Consumer, null, function (value) {
    return /*#__PURE__*/React.createElement(StyledIcon, {
      style: style,
      onClick: function onClick() {
        return handleClick(value);
      },
      className: classNames('iconfont', type),
      active: value.active === index,
      index: index,
      shape: shape
    });
  });
};

KpIcon.displayName = 'KpIcon';
export default KpIcon;